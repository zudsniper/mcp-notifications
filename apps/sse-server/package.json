{
  "name": "@mcp-notifications/sse-server",
  "version": "3.0.0",
  "description": "SSE server for MCP Notifications on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@mcp-notifications/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^3.94.0",
    "typescript": "^5.8.3"
  }
}